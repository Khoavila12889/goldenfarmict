# ─── Cấu hình Database ────────────────────────────────────────
# Local PC:   COMMENT dòng dưới (giữ nguyên #) → tự dùng path tuyệt đối
# VPS/Docker: UNCOMMENT (xóa #) → dùng relative path trong container
# DATABASE_URL=sqlite:///backend/company.db
